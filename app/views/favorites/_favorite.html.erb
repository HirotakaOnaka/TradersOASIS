<% if user_signed_in? %>
  <% if @favorite_hash.has_value?(trading_method.id) %>
    &nbsp;&nbsp;<%= link_to image_tag("heart_red.png"), trading_method_favorite_path(trading_method.id,@favorite_hash.key(trading_method.id)), method: :delete, class:"icon-button", id: "favorite-button", remote: true %>&nbsp;&nbsp;
  <% else %>
    &nbsp;&nbsp;<%= link_to image_tag("heart_gray.png"), trading_method_favorites_path(trading_method_id: trading_method.id), method: :post, class:"icon-button", id: "favorite-button", remote: true %>&nbsp;&nbsp;
  <% end %>
<% end %>